services:
  tic-tac-toe-front:
    container_name: tic-tac-toe-front-container
    build: .
    image: tic-tac-toe-front:v1
    ports:
      - 80:80
    expose:
      - 80
    networks:
      - internalnet

networks:
  internalnet:
    driver: bridge